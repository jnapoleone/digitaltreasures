/**
 * AngularJS module to process a form.
 */
var Dt = angular.module('dt',[]);
Dt.controller('dtLeads', function ($scope) {
  $scope.submitForm = function ( isValid ) {

//    if( isValid ) {
//      alert( 'The form is valide.' );
//    }
  }
});